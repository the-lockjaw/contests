#include <bits/stdc++.h>
using namespace std;

string generateTag(string caption)
{
} 

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
    }
    return 0;
}